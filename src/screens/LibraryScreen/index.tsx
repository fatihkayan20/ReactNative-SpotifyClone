import * as React from 'react';
import {StyleSheet, View} from 'react-native';

interface LibraryScreenProps {}
const styles = StyleSheet.create({});

export const LibraryScreen: React.FunctionComponent<LibraryScreenProps> =
  () => {
    return <View></View>;
  };
